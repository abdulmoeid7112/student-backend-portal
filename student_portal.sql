CREATE DATABASE student_portal;

use student_portal;

CREATE TABLE Student  (
     id         VARCHAR(50),
     name       VARCHAR(50),
     age        INT,
     level      VARCHAR(50),
     phone      VARCHAR(50)
  );

